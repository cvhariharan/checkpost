{{.Title}} — {{.Kind}} ({{.Severity}})

Host | Severity | Summary | Resolution
{{range .Items}}{{.Host}} | {{if .Severity}}{{.Severity}}{{else}}-{{end}} | {{.Summary}} | {{if .Resolution}}{{.Resolution}}{{else}}-{{end}}
{{end}}